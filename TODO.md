# SmartFlight TODO

这个 TODO 以当前仓库的实际实现状态为基线，优先列“还没闭环”或“需要真机验证”的事项。

## P0 当前优先

- [ ] 补齐 `README` 中提到的实际安装与接入流程截图或录屏
- [ ] 在真机上完整验证 `Shizuku` 链路下的飞行模式切换、移动数据切换、Wi-Fi 状态保留、蓝牙状态保留
- [ ] 在真机上完整验证 `Root` 链路下的同一组控制动作，并记录机型/系统差异
- [ ] 明确 `ADB 初始化` 的产品边界，确认它到底是一次性引导链路，还是后续仍需依赖外部能力
- [ ] 为 `AutomationForegroundService` 增加开机恢复和应用更新后恢复逻辑
- [ ] 为运行时自动化主链路补一轮端到端手工测试：启动目标应用、离开目标应用、息屏、亮屏、解锁、切换 Wi-Fi

## P1 规则与运行时

- [ ] 检查 `AutomationRuleEngine` 是否存在重复规则分支，收敛“非目标应用断网”和“离开目标应用断网”的重复判断
- [ ] 为“亮屏后不自动恢复联网”“解锁后不自动恢复联网”补充独立规则测试，避免行为只停留在设置层
- [ ] 持久化更多待恢复运行态，例如最近待执行断网时间点，减少服务被杀后的状态丢失
- [ ] 处理前台应用监听在不同 ROM 上的精度和延迟问题，必要时增加诊断提示
- [ ] 评估前台轮询间隔是否需要按场景动态调整，平衡耗电与响应速度
- [ ] 为“当前已处于目标联网状态”的跳过分支增加更清晰的用户可见解释

## P1 接入与权限

- [ ] 完善接入检查页文案，区分“未授权”“不支持”“执行失败”“已授权但不可用”
- [ ] 为 `autoGrantCompanionPermissions()` 增加失败反馈，而不是只刷新状态
- [ ] 梳理 Android 13+ 通知权限、Android 12+ 蓝牙权限、使用情况访问权限的异常路径
- [ ] 增加“为什么不能进入主界面”的聚合说明，减少用户逐项排查成本

## P1 应用范围管理

- [ ] 为应用扫描增加安装/卸载/更新后的增量刷新，而不是只依赖手动刷新
- [ ] 校验联网列表、白名单、黑名单在多次扫描后的优先级是否始终正确
- [ ] 增加“仅显示已联网应用”“仅显示手动规则应用”等更贴近排障的筛选方式
- [ ] 评估是否需要展示最近一次扫描时间和扫描失败原因

## P2 日志与诊断

- [ ] 为执行日志补充 `matchedRules`、控制模式、执行前后状态差异，提升排障价值
- [ ] 提供日志导出能力，至少支持文本或 JSON
- [ ] 区分“手动诊断动作”和“自动化真实动作”，避免日志语义混淆
- [ ] 在诊断页增加更直接的执行器原始输出展示入口

## P2 测试

- [ ] 为 `AutomationForegroundService` 的调度逻辑增加可测试抽象，减少只能靠真机验证的部分
- [ ] 为 `DefaultAccessRepository` 的状态写回逻辑补单元测试
- [ ] 为应用扫描与手动覆盖规则补单元测试
- [ ] 为 Wi-Fi 例外规则、黑名单优先级、延迟断网取消逻辑补完整测试矩阵
- [ ] 跑通基础 `assembleDebug` 和 `test`，并把结果固定到仓库流程里

## P3 产品化与发布准备

- [ ] 补充首版 README 的截图、限制说明和常见问题
- [ ] 整理版本号、签名配置、发布渠道配置
- [ ] 增加隐私说明，解释为什么需要 `QUERY_ALL_PACKAGES`、使用情况访问和前台服务
- [ ] 准备首个可安装测试包，并建立最小变更日志
